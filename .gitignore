# OS / editor
.DS_Store
Thumbs.db
*.swp
*~

# Dependencies / build (build-free for now; kept for future Phase 1.5)
node_modules/
vendor/
build/

# Logs
*.log

# Eval runner: cached provider responses are committed on purpose (they make a
# run reproducible and let scorers be fixed without re-spending tokens), so
# nothing here is ignored. Listed for the next person who wonders.
