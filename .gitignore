# OS / editor
.DS_Store
Thumbs.db
*.swp
*~

# Dependencies / build (build-free for now; kept for future Phase 1.5)
node_modules/
vendor/
build/

# Logs
*.log

# Eval runner: nothing is persisted. No response cache, no run records — a run
# prints its result and that is the artifact. Listed for the next person who
# wonders why evals/ holds only cases.json.
